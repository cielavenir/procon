#include <iostream>
#include <string>
#include <queue>
#include <map>
#include <cstdlib>
using namespace std;

int main(){
	string line;
	for(;getline(cin,line);){
		int N=10;
		vector<vector<char>>v(n);
		int idx=0;
		for(int y=0;y<n;y++){
			v[z][y].resize(n);
			for(int x=0;x<n;x++){
				v[y][x]=line[idx++];
			}
		}
		//due to "prism", bfs should be applied?
	}
}