#include <string>
#include <map>
#include <iostream>
using namespace std;
int main(){
	map<string,int>m;
	string s;
	int N,M,f=0;
	for(cin>>N;N--;m[s]=1)cin>>s;
	for(cin>>M;M--;cout<<s<<endl)if(cin>>s,m[s])cout<<(f?"Closed by ":"Opened by "),f^=1;else cout<<"Unknown ";
}